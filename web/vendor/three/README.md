Three.js 0.170.0 (MIT). Vendored from the published `three@0.170.0` npm package.
Only the two controls' bare `three` imports are changed to a local relative URL.
Runtime requires no CDN. See LICENSE for attribution and terms.
